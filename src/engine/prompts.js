// prompts.js — system prompts + the shared TASK_RULES (CLAUDE.md rule 4).
//
// Placeholder (Chunk 1). The prompts currently live INLINE in
// NexusPathEngine.jsx. Chunk 5 extracts them here. The task rules
// (session-sized ~2-4h, concrete first action, verifiable doneCriterion)
// apply to every prompt that emits tasks and must live in one place.
export {}
