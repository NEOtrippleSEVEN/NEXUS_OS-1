// schema.js — engine state shapes + runtime validation.
//
// Placeholder (Chunk 1). Shapes are currently implicit in NexusPathEngine.jsx.
// This file will document/validate the engine object: brief, milestones
// (effortHours + computed dates), tasks (firstAction, doneCriterion), the
// completion log, and the simToday / baselineHorizon date fields — and assert
// that LLM responses carry NO dates (rule 1) before they enter state.
export {}
